extends layout

block content
  script.
    function animateValue(id, start, end, duration) {
      var range = end - start;
      var current = start;
      var increment = end > start ? 1 : -1;
      var stepTime = Math.abs(Math.floor(duration / range));
      var obj = document.getElementById(id);
      var timer = setInterval(function () {
        current += increment;
        obj.innerHTML = current;
        if (current === end) {
          clearInterval(timer);
        }
      }, stepTime);
    }
  //.pb-2.mt-2.mb-4
  .row
    .col-md-12.qb
      h1 Результаты
      img.half(src='/images/sport_badges-02-512.png')
      //h3 Вы получили &nbsp;
      h3
      div#value 120
      h3 баллов
      p
      p
      p
      h3 Поздравляем!
      if (!user)
        p Зарегистрируйтесь, чтобы сохранить результат и получить доступ к базовому бесплатному курсу.
      script(src='https://yastatic.net/es5-shims/0.0.2/es5-shims.min.js')
      script(src='https://yastatic.net/share2/share.js')
      .ya-share2(data-services='collections,vkontakte,facebook,odnoklassniki,moimir')

      //button.btn.btn-primary(type='submit')
      //  i.far.fa-envelope.fa-sm
      //  | Регистрация
      if !user
        a.btn.btn-primary.nav-link(href='/signup') Регистрация
        p
        iframe(width='100%', height='500', src='https://www.youtube.com/embed/c4-H_kC3T8Y', frameborder='0', allow='autoplay; encrypted-media', allowfullscreen='')
    script.
      sessionStorage.removeItem('counterQuestions');
      animateValue("value", 0, 120, 500);
